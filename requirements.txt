# Deprecated: use `uv sync` and pyproject.toml for dependencies.
# Kept for occasional `uv pip install -r` workflows only.

pandas
python-dotenv
openpyxl
requests
urllib3
fastapi
websockets
uvicorn
jinja2
openai
langchain-core
langchain-openai
langgraph

# Archived reference

`legacy_src/` holds a **frozen snapshot** of the pre-rewrite codebase for **human reference** (behavior, comparisons, and migration notes).

**Do not import or reuse this code in the new implementation.** The runnable app is only the top-level `src/` package managed with [`uv`](https://docs.astral.sh/uv/) and [`pyproject.toml`](../pyproject.toml).

If you need historical behavior, read the files here or compare logs; do not copy modules into production paths.

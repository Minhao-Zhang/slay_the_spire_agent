# Greenfield implementation (archived)

This tree is the **quarantined rewrite** (former top-level `src/`): `control_api`, LangGraph, `domain`, etc. It is **not** the active runtime.

**Current app:** legacy package at repo root [`src/`](../../src) and [`ARCHITECTURE.md`](../../ARCHITECTURE.md).

**Policy:** Treat this directory as read-only reference when porting ideas or comparing behavior.

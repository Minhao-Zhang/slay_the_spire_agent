# Greenfield implementation (`src/`)

This directory is the **only** Python package wired to `uv run` and `python -m src.main`.

Design and stages: [`docs/restart/README.md`](../docs/restart/README.md) and [`docs/restart/ARCHITECTURE.md`](../docs/restart/ARCHITECTURE.md).

**Policy:** `archive/legacy_src/` is **reference only** — do not import or paste code from there into this tree.
